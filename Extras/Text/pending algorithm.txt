@app.route('/get_list')
def get_list():
    total_rows = get_total_rows()
    print("This is the total number of digit without floating point for approval send get link number",last_digit)
    return str(total_rows)

modify this peice of code that upon receivement it will receive a text too 

approve_send_items_bar
approve_receive_items_bar
receive_items_bar

so if it recieves any text among these three then it should pass the appropriate excel to the function, the excels are 

receive_items_excel='Excel/receive_items.xlsx'
approve_send_excel='Excel/approve_send.xlsx'
approve_receive_excel='Excel/approve_receive.xlsx'

so like just make this algorithm

if text == approve_send_items_bar
   pass_excel=approve_send_excela
else if text == approve_receive_items_bar
   pass_excel=approve_receive_excel
else if text == receive_items_bar
   pass_excel=receive_items_excel
